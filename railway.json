{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "npm install && cd client && npm install && npm run build && cd ../server && npm install && npm run build"
  },
  "deploy": {
    "startCommand": "cd server && npx prisma db push --skip-generate && npx prisma db seed && NODE_ENV=production node dist/server.js",
    "healthcheckPath": "/api/health",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 3
  }
}
